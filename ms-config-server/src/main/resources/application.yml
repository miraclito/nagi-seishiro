server:
  port: 7070


spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/miraclito/nagi-seishiro.git # ruta del repositorio git
          searchPaths: config-data
          username: root
          password: 123456
          default-label: gateway-server

